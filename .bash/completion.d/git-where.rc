# Bash completion loader for git-where
GIT_TOOLS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../src/git-tools" && pwd)"
if [[ -f "${GIT_TOOLS_DIR}/git-where/completion.bash" ]]; then
    source "${GIT_TOOLS_DIR}/git-where/completion.bash"
fi
