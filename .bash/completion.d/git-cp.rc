# Bash completion loader for git-cp
GIT_TOOLS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../src/git-tools" && pwd)"
if [[ -f "${GIT_TOOLS_DIR}/git-cp/completion.bash" ]]; then
    source "${GIT_TOOLS_DIR}/git-cp/completion.bash"
fi
